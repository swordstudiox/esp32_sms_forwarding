# 主要更新

- 同步上游 2026-08-27 的三项修复：恢复 Telegram TLS 相关证书链兼容、恢复 modem task 所需 include、增强 SIM ICCID 读取兜底。
- ICCID 读取增加标准 `AT+CRSM` 读取 EF_ICCID 的后备路径，覆盖部分厂商 ICCID 命令不可用或锁卡状态下的识别场景。
- TLS 配置保留本 fork 的 `CONFIG_MBEDTLS_SSL_RENEGOTIATION`，并合入上游交叉签名证书链校验配置；ESP-IDF 5.5.4 下该交叉签名选项会提示未知但不影响构建。
- README 补充 eSIM Profile 可通过 Activation Code 下载并写入；该能力已在真实 eUICC 上实测写入成功。
- 保持上游基线标识为 `v1.1.5`，版本号更新为 `1.3.2-fork.v1.1.5`。

# 验证

- Python 回归测试。
- ESP-IDF 5.5.4 本地构建验证。
